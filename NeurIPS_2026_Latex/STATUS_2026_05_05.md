# DUET paper — status snapshot at 2026-05-05 evening

**TL;DR**: full paper drafted (Intro/RW/Method/Experiments/Discussion/Conclusion + 4 appendices), all tables/figures wired, two figures rendered, advisor's framing fully adopted, two more ablation cells filled (1.5B-WS -baseline_sep collapse confirmed). Page budget tight (~9pp main); ready for compile + figure-1 swap-in + final ablation cells.

---

## What was done since the planning doc

### Replaced files (advisor's versions are now canonical)
- `sections/01_introduction.tex` — adopted advisor's mentor draft verbatim.
- `sections/02_related_work.tex` — already advisor's version, wired into `neurips_2026.tex`.
- `sections/duet_references.bib` — wired via `\bibliography`; all 18 keys verified, zero missing.
- `tables/main_results.tex` — bold rule fixed (best per column bold; strongest non-DUET baseline underlined; Δ row over underlined). DUET\* notation dropped; cells unchanged.

### Newly written
- `sections/03_method.tex` — full §3 rewrite around Stage 1 (Correct = BS + DR3) / Stage 2 (Extract = action BC + state SC). Six subsections (3.1 setup, 3.2 biases, 3.3 overview, 3.4 BS, 3.5 DR3, 3.6 BC, 3.7 SC, 3.8 combined update). **DR3 direction unified to** $\hat w = \pi_\theta/\pi_\beta$ (= $D/(1-D)$) per code; "self-attenuation" is the discriminator-strengthening story, not "$\hat w \to 1$".
- `sections/04_experiments.tex` — §4.1 setup, §4.2 main results, §4.3 ablation framework, §4.4 self-attenuation dynamics (refs Figure 3), §4.5 reproducibility note. Uses **corrected empirical numbers** from the actual log: $\hat w_\tau$ falls $\approx 1.0 \to \approx 0.67$ (a $\approx 33\%$ teacher-gradient down-weighting), not the "$0.50 \to 0.05$" I had in the planning doc.
- `sections/05_discussion.tex` — single-seed CI, scope of DR3 (honest negative on 1.5B-AF), cross-infra LUFFY variance, token-level DR3 as future work, model/env scope.
- `sections/06_conclusion.tex` — one paragraph; restates "correct teacher replay before exploiting it".
- `tables/ablation.tex` — Table 2 with `Mechanism role` column (Stage 1 / Stage 2 mapping). 4 cells filled (1.5B-AF/-WS × -DR3/-baseline_sep), the rest TBD.
- `tables/app_sensitivity.tex` — μ-schedule mini-sweep on 1.5B-WS (9 verified rows).
- `sections/appendix_a_luffy.tex` — LUFFY 3-way reproducibility (49.5% / 38.0% / 3.5%), neutral framing.
- `sections/appendix_b_sensitivity.tex` — wraps the sensitivity table.
- `sections/appendix_c_impl.tex` — DR3 disc, EMA inference, dual ESS, FSDP sync, BC schedule SOTA table per cell, SC hash + grpo_decouple, baseline-sep config, software stack.
- `sections/appendix_d_reward.tex` — reward-mean joint table (uses existing `main_results_with_reward.tex`).
- `sections/appendix.tex` — wrapper that orders A → B → C → D.
- `figures/make_figures.py` — matplotlib script; renders Fig 2 + Fig 3 from training logs.
- `figures/fig3_self_attenuation.{pdf,png}` — **rendered**. 3 panels: ŵ_τ, μ(t), disc_acc.
- `figures/fig2_training_curves.{pdf,png}` — rendered but **NOT included in main paper** (training-time on-policy SR doesn't reflect val SR — would tell wrong story; safer to drop).

### New empirical updates (since planning doc)
- **1.5B-WS -baseline_sep finished at 20:51** → **0.0%** strict, 0.0% lenient, reward $-0.1$. Both 1.5B cells now collapse with Bias 1 removed — much stronger evidence. Already patched into Table 2 + §4.3 paragraph.
- 5/8 ablation cell (1.5B-AF -BC) now running on the queue.
- Figure 3 numbers patched in §3.5 + §4.4 to match actual log values.

### Static checks (done)
- All `\input{}` resolve. ✅
- Zero duplicate `\label{}`s. ✅
- Only undefined ref: `fig:method` (Figure 1 — placeholder block added; user is making the actual image with nanobanana). ✅
- All `\citep{}` keys exist in bib. ✅
- No marketing language ("fix LUFFY", "self-calibrating", "JSD-driven") in prose. ✅
- ŵ direction consistent throughout as $\pi_\theta/\pi_\beta$. ✅
- DUET\* notation dropped; "DUET" everywhere. ✅

### Page budget
| Section | Words | Est. pages (700 wpp) |
|---|---:|---:|
| §1 Intro | 841 | 1.20 |
| §2 RW | 714 | 1.02 |
| §3 Method | 2454 + 9 equations + Figure 1 | 4.0–4.3 |
| §4 Experiments | 864 + Table 1 + Table 2 + Figure 3 | 2.4–2.6 |
| §5 Discussion | 375 | 0.54 |
| §6 Conclusion | 199 | 0.28 |
| **Main paper total** | | **9.4–9.9** |
| Abstract (locked) | 211 | 0.35 |
| Appendix A–D | ~900 | 1.5–2 |

Main paper is ~0.5pp over the 9-page limit when you compile. **Where to trim if needed (priority order)**:
1. §3 method paragraphs that are pointers to App.C (e.g. DR3 stability "(i)/(ii)/(iii)" list — can be one sentence "see App.C").
2. §1 intro paragraph 4 (DUET intro) — contains some redundancy with §3.3 overview.
3. §2 RW — the experience-replay/demonstrations paragraph could be one short paragraph.
4. §3.8 combined update — equation is dense; can be inline.

Do not trim §3.5 (DR3) — it's the load-bearing mechanism explanation. Do not trim §4.3 ablation discussion — it's the strongest empirical anchor.

---

## Open items for user (when back)

1. **Figure 1** — placeholder block added in §3.3 referencing `figures/fig1_method.pdf`. Drop in your nanobanana image at that path and the ref resolves automatically.
2. **First compile pass** — run `pdflatex` (or your usual NeurIPS toolchain). I couldn't compile locally (no LaTeX installed). Likely first-pass issues: Figure 1 file missing (placeholder caption only), bibliography needs `bibtex`/`biber` pass.
3. **Page count check after compile** — if 9.5+ pp, apply trimming priority list above.
4. **Remaining ablation cells** — 1.5B-AF -BC is currently running (cell 5/8). Cells 6/7/8 (-BC WS, -SC AF, -SC WS) and 8 L20X 3B cells will fill the remaining `\textsc{tbd}` entries in Table 2. Update `tables/ablation.tex` directly when each cell finishes.
5. **App.C "1.5B-WS uses CHORD" sanity** — the agent flagged that one config file (`webshop_qwen1.5b_duet.yaml`, an old/test file) has `use_chord: false`. The actual SOTA config we use (`webshop_qwen1.5b_duet_swC_02_pk03_v10_floor06.yaml`) has `use_chord: true` (verified earlier). App.C correctly reflects the SOTA config; no action needed but worth a re-glance.

---

## File map (final)

```
NeurIPS_2026_Latex/
├── neurips_2026.tex          # main entry; \input order: abstract, §1, §2, §3, §4, §5, §6, refs, appendix
├── abstract.tex              # LOCKED (deadline passed)
├── checklist.tex             # NeurIPS checklist (TODO: fill before submit)
├── sections/
│   ├── 01_introduction.tex   # advisor draft (canonical)
│   ├── 02_related_work.tex   # advisor draft (canonical)
│   ├── 03_method.tex         # full §3, Stage1/Stage2 framing
│   ├── 04_experiments.tex    # §4.1–§4.5
│   ├── 05_discussion.tex     # §5
│   ├── 06_conclusion.tex     # §6
│   ├── appendix.tex          # wrapper: A → B → C → D
│   ├── appendix_a_luffy.tex
│   ├── appendix_b_sensitivity.tex
│   ├── appendix_c_impl.tex
│   ├── appendix_d_reward.tex
│   └── duet_references.bib   # 18 entries, all used
├── tables/
│   ├── main_results.tex      # Table 1
│   ├── main_results_with_reward.tex  # extended (App.D)
│   ├── ablation.tex          # Table 2 (filling)
│   └── app_sensitivity.tex   # App.B mini-sweep
├── figures/
│   ├── fig3_self_attenuation.{pdf,png}  # READY for inclusion
│   ├── fig2_training_curves.{pdf,png}   # generated; NOT included in paper
│   └── make_figures.py
├── data/
│   ├── ablation_results.md   # auto-updated by orchestrator
│   └── raw_data.md
├── ABLATION_PLAN.md
├── PAPER_PLAN.md             # earlier plan; partly superseded by this STATUS doc
├── narrative.md              # 2026-05-04 framing decisions (still valid)
└── STATUS_2026_05_05.md      # ← you are here
```
