% ============================================================================
% Appendix D — Extended main results (with reward mean)
% ============================================================================

\section{Extended main results: reward mean}
\label{app:reward_table}

Table~\ref{tab:main_results} reports strict success rate (score $\geq 1.0$) for compactness. WebShop also supports partial credit; we report reward mean on the cell where the two metrics are most distinct: 1.5B-WebShop. ALFWorld is binary, so its reward mean coincides with strict success rate.

\input{tables/main_results_with_reward}

The ranking under reward mean matches the ranking under strict success rate, indicating DUET's advantage on WebShop is not driven by a small number of high-credit successes.
